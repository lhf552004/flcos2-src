import { DynamicFormTableModalLinkDirective } from './dynamic-form-table-modal-link.directive';

describe('DynamicFormTableModalLinkDirective', () => {
  it('should create an instance', () => {
    const directive = new DynamicFormTableModalLinkDirective();
    expect(directive).toBeTruthy();
  });
});
