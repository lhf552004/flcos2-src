import { DynamicFormModalLinkDirective } from './dynamic-form-modal-link.directive';

describe('DynamicFormModalLinkDirective', () => {
  it('should create an instance', () => {
    // const directive = new DynamicFormModalLinkDirective();
    // expect(directive).toBeTruthy();
  });
});
